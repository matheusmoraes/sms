'use strict'

const TextToSMSParser = require('../../models/sms');
const expect = require('chai').expect;


describe('TextToSMSParser module', () => {
  it('should export a function', () => {
    expect(TextToSMSParser).to.be.a('function');
  });

  describe('module', () => {
    let parser;

    beforeEach(() => {

    });

  });
});
